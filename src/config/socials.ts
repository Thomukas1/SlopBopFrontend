// The project's own social accounts — app-wide, not any one feature's. Lived in
// AboutPage until the commission page needed the same links; a feature reaching
// into another feature's folder for them would be the wrong shape.
export const PROJECT_SOCIALS: Record<string, string> = {
  instagram: 'https://www.instagram.com/slopboptv/',
  tiktok: 'https://www.tiktok.com/@slopboptv',
};
